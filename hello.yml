config:
  target: "http://localhost:3005"
  phases:
    - duration: 60
      arrivalRate: 10
    - duration: 60
      arrivalRate: 50
    - duration: 60
      arrivalRate: 100
    - duration: 60
      arrivalRate: 200
    - duration: 60
      arrivalRate: 400
    - duration: 60
      arrivalRate: 800
    - duration: 60
      arrivalRate: 1000
  processor: "./test/idGenerator.js"
scenarios:
  - flow:
      - function: "idGenerator"
      - get:
          url: "/story/{{id}}"
      - get:
          url: "/risksandchallenges/{{id}}"
      - get:
          url: "/environmentalcommitments/{{id}}"
